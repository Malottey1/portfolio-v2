---
---

<section class="section" id="about">
	<div class="container about">
		<div>
			<h2 class="section-heading">About</h2>
			<p class="about__bio">
				Placeholder short bio goes here — a few sentences on background, focus areas, and what
				kind of role I'm looking for. Real copy lands in Stage 2.
			</p>
		</div>
		<a class="about__resume" href="#" download>Download Resume</a>
	</div>
</section>

<style>
	.about {
		display: flex;
		flex-direction: column;
		align-items: flex-start;
		gap: var(--space-8);
	}

	.about__bio {
		max-width: 60ch;
		color: var(--color-text-secondary);
	}

	.about__resume {
		display: inline-block;
		padding: var(--space-3) var(--space-6);
		border: 1px solid var(--color-border);
		border-radius: var(--radius-md);
		text-decoration: none;
		font-size: var(--text-sm);
		font-weight: 600;
	}
</style>
